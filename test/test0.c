#include <stdio.h>
#include <cs50.h>

int main(void)
{
    long long num;
    int count;

    do
    {
        count = 0;
        printf("Num: ");
        scanf("%lld", &num);
        long long no = num;
        n /= 10
        count++;

    }
    while(count != 13; count != 15; count != 16)



}